%h1= "Edit #{@app.name}"
.row
  .col-md-8
    = form_for @app do |f|
      .form-group
        = f.label :name
        = f.text_field :name, class: 'form-control'
      .form-group
        = f.submit "Save", class: 'btn btn-success'
